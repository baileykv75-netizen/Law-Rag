# Frontend

This directory will contain the local browser UI.

Current task target: React + Vite + TypeScript.

Frontend responsibilities are limited to presentation and user interaction. Legal reasoning, OCR logic, retrieval logic, secret management, and audit-domain decisions belong in backend/application layers.
