name: Law-Rag Stage 19.3 Windows Safe Updates

on:
  pull_request:
    branches:
      - stage19-2-signing-semantics
  workflow_dispatch:

permissions:
  contents: read

jobs:
  windows-safe-updates:
    runs-on: windows-latest
    timeout-minutes: 60

    steps:
      - name: Check out exact source head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0

      - name: Assert exact PR head checkout
        if: github.event_name == 'pull_request'
        shell: pwsh
        run: |
          $actual = (git rev-parse HEAD).Trim()
          $expected = '${{ github.event.pull_request.head.sha }}'
          if ($actual -ne $expected) { throw "Expected exact PR head $expected, got $actual" }
          Write-Host "Exact PR head: $actual"

      - name: Set up CPython 3.12.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.12.10'

      - name: Set up Node 22.23.2
        uses: actions/setup-node@v4
        with:
          node-version: '22.23.2'
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Build exact Windows onedir bundle
        shell: pwsh
        run: ./release/build-windows.ps1

      - name: Revalidate Stage 18 packaged boundaries
        shell: pwsh
        run: ./release/smoke-stage18-5-windows.ps1 -BundleDir ./release/dist/Law-Rag -Port 8893

      - name: Build inherited Stage 19.1 unsigned installer
        shell: pwsh
        run: ./release/build-installer.ps1

      - name: Revalidate Stage 19.1 install lifecycle
        shell: pwsh
        run: ./release/smoke-stage19-1-installer.ps1

      - name: Revalidate Stage 19.2 unsigned publication refusal
        shell: pwsh
        run: |
          Remove-Item Env:LAW_RAG_RELEASE_SIGNER_THUMBPRINT -ErrorAction SilentlyContinue
          $refused = $false
          try {
            ./release/verify-stage19-2-signing.ps1 -EvidencePath ./release/installer-dist/STAGE19-3-INHERITED-SIGNING-EVIDENCE.json -RequirePublishable
          } catch {
            $refused = $true
            Write-Host "Expected inherited unsigned refusal: $($_.Exception.Message)"
          }
          if (-not $refused) { throw "Inherited Stage 19.2 publication gate unexpectedly allowed an unsigned build." }

      - name: Stage 19.3 create primary CI signer in My store
        timeout-minutes: 3
        shell: pwsh
        run: ./release/stage19-3-ci-signing-identities.ps1 -Phase CreatePrimary

      - name: Stage 19.3 trust primary signer in Root
        timeout-minutes: 2
        shell: pwsh
        run: ./release/stage19-3-ci-signing-identities.ps1 -Phase TrustPrimaryRoot

      - name: Stage 19.3 trust primary signer in TrustedPublisher
        timeout-minutes: 2
        shell: pwsh
        run: ./release/stage19-3-ci-signing-identities.ps1 -Phase TrustPrimaryPublisher

      - name: Stage 19.3 create mismatch CI signer in My store
        timeout-minutes: 3
        shell: pwsh
        run: ./release/stage19-3-ci-signing-identities.ps1 -Phase CreateMismatch

      - name: Stage 19.3 trust mismatch signer in Root
        timeout-minutes: 2
        shell: pwsh
        run: ./release/stage19-3-ci-signing-identities.ps1 -Phase TrustMismatchRoot

      - name: Stage 19.3 trust mismatch signer in TrustedPublisher
        timeout-minutes: 2
        shell: pwsh
        run: ./release/stage19-3-ci-signing-identities.ps1 -Phase TrustMismatchPublisher

      - name: Stage 19.3 finalize CI signing state
        timeout-minutes: 2
        shell: pwsh
        run: ./release/stage19-3-ci-signing-identities.ps1 -Phase Finalize

      - name: Stage 19.3 real installer positive trust chain
        timeout-minutes: 8
        shell: pwsh
        run: ./release/smoke-stage19-3-safe-update-v3.ps1 -Phase Positive

      - name: Stage 19.3 real installer tamper refusal
        timeout-minutes: 4
        shell: pwsh
        run: ./release/smoke-stage19-3-safe-update-v3.ps1 -Phase Tamper

      - name: Stage 19.3 prepare deterministic unsigned small-fixture baseline
        timeout-minutes: 4
        shell: pwsh
        run: ./release/stage19-3-ci-fixture-and-cleanup.ps1 -Phase PrepareFixture

      - name: Stage 19.3 detached CMS tamper refusal
        timeout-minutes: 3
        shell: pwsh
        run: ./release/smoke-stage19-3-safe-update-v3.ps1 -Phase ManifestTamper

      - name: Stage 19.3 signer mismatch refusal
        timeout-minutes: 4
        shell: pwsh
        run: ./release/stage19-3-ci-fixture-and-cleanup.ps1 -Phase SignerMismatch

      - name: Stage 19.3 version monotonicity refusal
        timeout-minutes: 3
        shell: pwsh
        run: ./release/smoke-stage19-3-safe-update-v3.ps1 -Phase SameVersion

      - name: Stage 19.3 HTTPS boundary refusal
        timeout-minutes: 3
        shell: pwsh
        run: ./release/smoke-stage19-3-safe-update-v3.ps1 -Phase HttpUrl

      - name: Stage 19.3 cleanup CI-only signing identities
        if: always()
        timeout-minutes: 3
        shell: pwsh
        run: ./release/stage19-3-ci-fixture-and-cleanup.ps1 -Phase Cleanup

      - name: Upload Stage 19.3 validation artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: law-rag-stage19-3-safe-update-evidence
          path: |
            release/update-dist/**/*.json
            release/update-dist/**/*.p7s
            release/installer-dist/STAGE19-3-INHERITED-SIGNING-EVIDENCE.json
          retention-days: 7
          if-no-files-found: warn
