# Agent

This directory is reserved for the future constrained audit Agent/orchestration layer.

Do not implement Agent behavior before the OCR, canonical document model, deterministic rules, legal retrieval, and primary LLM interfaces have reliable tool boundaries.

The Agent must use an explicit allowlist of tools and may not bypass mandatory audit pipeline stages defined by application code.
