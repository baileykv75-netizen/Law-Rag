[pytest]
markers =
    ocr_smoke: opt-in integration test requiring local PaddleOCR dependencies/models
    rag_semantic_smoke: opt-in integration test requiring local sentence-transformers and BGE model
    deepseek_smoke: opt-in paid/network integration test requiring DEEPSEEK_API_KEY
    kimi_smoke: opt-in paid/network integration test requiring MOONSHOT_API_KEY
